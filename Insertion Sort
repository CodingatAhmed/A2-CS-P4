def InsertionSort():
    global array
    for i in range(len(array) - 1):
        while array[i] > array[i + 1] and i >= 0:
            array[i],array[i + 1] = array[i + 1],array[i]
            i -= 1
    return array
